<div class="container">
    <div class="row">
    <div class="col-sm-10 col-md-6">   
         <br><br><br>
			<h2>Olvidaste tu contraseña?</h2>

				<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
				  <%= devise_error_messages! %>

				  <div class="field">
				    <%= f.label :email, "Correo" %><br />
				    <%= f.email_field :email, autofocus: true, class: "form-control", placeholder: "Introduce tu correo"  %>
				  </div>

				  <div class="actions">
				    <%= f.submit "Enviar las instrucciones a mi correo" %>
				  </div>
				<% end %>

<%= render "devise/shared/links" %>
 </div>
</div>